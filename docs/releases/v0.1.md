# V0.1 — Universal Application and Session Foundation

**Release status:** In progress; no tag

**Last completed acceptance step:** Phase E — PostgreSQL persistence

**Next planned phase:** Phase F — not started

`v0.1.0` is created only after every phase below has working code and its acceptance evidence is
recorded. Completing repository or Compose setup is not a release.

| Phase | Deliverable | Status | Evidence |
| --- | --- | --- | --- |
| A | Standalone repository, buildable hosts, source CI, one-way mirror | Complete | [Phase A record](../implementation-notes/v0.1-phase-a-repository-foundation.md) |
| B | PostgreSQL, Redis, Mailpit, Compose, native health checks | Complete | [Phase B record](../implementation-notes/v0.1-phase-b-local-runtime.md) |
| C | Concrete backend/module skeleton and dependency rules | Complete | [Phase C record](../implementation-notes/v0.1-phase-c-backend-module-boundaries.md) |
| D | Core V0.1 domain model and invariants | Complete | [D.1 record](../implementation-notes/v0.1-phase-d1-application-profile-foundation.md); [D.2 record](../implementation-notes/v0.1-phase-d2-user-account-foundation.md); [D.3 record](../implementation-notes/v0.1-phase-d3-authentication-transaction-foundation.md); [D.4 record](../implementation-notes/v0.1-phase-d4-session-foundation.md); [D.5 record](../implementation-notes/v0.1-phase-d5-security-event-foundation.md); [D.6 record](../implementation-notes/v0.1-phase-d6-notification-outbox-foundation.md) |
| E | EF Core/PostgreSQL persistence, constraints, protection, and migration | Complete | [Phase E record](../implementation-notes/v0.1-phase-e-postgresql-persistence.md) |
| F | Authentication transaction orchestration, evidence gating, and runtime lifecycle | Not started | — |
| G | Opaque server-managed session subsystem | Not started | — |
| H | Application and assurance policy contracts | Not started | — |
| I | Trusted security-event construction, redaction, query, retention, and authorization | Not started | — |
| J | Outbox workflow integration, claim/lease, provider delivery, retry, and replay | Not started | — |
| K | Minimal application-aware experience shell | Not started | — |
| L | V0.1 HTTP, secret, log, redirect, and abuse-control baseline | Not started | — |
| M | Integration, security, and end-to-end acceptance suites | Not started | — |
| N | V0.1 acceptance record and release decision | Not started | — |

## V0.1 exit gate

The eventual release must start the web, API, and dependency stack; resolve an application
profile; execute an authentication transaction lifecycle; issue and revoke a server-managed
session; enforce redirect and policy boundaries; persist audit/outbox records; and pass its defined
tests. Phases A through C supply the repository, local dependencies, and compile-time boundaries;
they satisfy none of the product-behavior items by themselves. D.1 defines valid application
configuration, D.2 defines valid account-state transitions, D.3 defines the central transaction's
purpose, lifetime, and legal state graph, D.4 defines session lifetime, verifier rotation,
revocation, and expiry invariants, and D.5 defines immutable security-event context and metadata.
D.6 defines the protected notification envelope and its due, retry, delivery, and
permanent-failure rules. Phase E adds persistence ports, mappings, repositories, constraints,
destination protection, a shared unit of work, and an accepted initial migration for those six
records. Its 20 integration and 98 security cases run against guarded disposable PostgreSQL
databases. No host registers that layer and no request invokes it.

## Phase D atomic ledger

| Step | Domain boundary | Status |
| --- | --- | --- |
| D.1 | `ApplicationProfile`, shared application/tenant identifiers, and web redirect invariants | Complete |
| D.2 | `UserAccount` identity root and account-state invariants | Complete |
| D.3 | `AuthenticationTransaction` state and transition invariants | Complete |
| D.4 | `Session` state, expiry, and revocation invariants | Complete |
| D.5 | Append-only `SecurityEvent` data boundary | Complete |
| D.6 | `NotificationOutbox` message and delivery-state invariants | Complete |

The ledger is a sequencing boundary, not six claims of running authentication capability. D.1
through D.6 now coexist as in-memory contracts without weakening the module dependency rules.

## Phase E atomic ledger

| Step | Persistence boundary | Status |
| --- | --- | --- |
| E.1 | EF Core/Npgsql foundation and design-time tooling | Complete |
| E.2 | `ApplicationProfile` and redirect persistence | Complete |
| E.3 | `UserAccount` persistence | Complete |
| E.4 | `AuthenticationTransaction` persistence | Complete |
| E.5 | `Session` persistence | Complete |
| E.6 | Append-only `SecurityEvent` persistence | Complete |
| E.7 | Notification-destination protection | Complete |
| E.8 | Notification outbox persistence and due query | Complete |
| E.9 | Shared unit of work and persistence registration | Complete |
| E.10 | Initial migration and persistence documentation | Complete |

The Phase E gate passed 118 database-backed cases: 20 integration cases and 98 security cases.
The full local backend run passed all 571 cases across ten projects, including both architecture
rules. The migration was applied, downgraded, and reapplied only in disposable databases; the
durable `authnexus` database and volume were untouched.

## Current gate

Phase F is the next implementation gate and has not started. Phase M also remains not started: the
Phase E suites accept the persistence layer, while Phase M is reserved for whole-product
integration, security, and end-to-end journeys after the intervening runtime phases exist. The
repository remains an in-progress V0.1 without a `v0.1.0` tag.
